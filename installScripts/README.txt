These scripts are to facilitate the install on OSX mac computers.
they should be copied into the parent directory of this repository and run from there.
./make_start_over.sh : Cleans and builds for OSX and iOS then installs opencv and runs the tests.